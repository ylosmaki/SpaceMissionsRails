<%= form_with(model: mission) do |form| %>
  <% if mission.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mission.errors.count, "error") %> prohibited this mission from being saved:</h2>

      <ul>
        <% mission.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :initiated, style: "display: block" %>
    <%= form.text_field :initiated %>
  </div>

  <div>
    <%= form.label :terminated, style: "display: block" %>
    <%= form.text_field :terminated %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
