<%= form_with(model: rocket) do |form| %>
  <% if rocket.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rocket.errors.count, "error") %> prohibited this rocket from being saved:</h2>

      <ul>
        <% rocket.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :mission_id, style: "display: block" %>
    <%= form.number_field :mission_id %>
  </div>

  <div>
    <%= form.label :first_launch, style: "display: block" %>
    <%= form.text_field :first_launch %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
